spring:
  datasource:
    url:
      jdbc:postgresql://localhost:5432/postgres?currentSchema=public
    username:
      postgres
    password:
      admin
    driver-class-name:
      org.postgresql.Driver
server:
  port: 5000
